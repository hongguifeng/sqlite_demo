# 第二章：SQLite 简介与环境搭建

本章正式进入 SQLite 本身，重点是先把开发环境跑通，并建立对 SQLite 使用方式的第一印象。

## 目录

1. [2.1 什么是 SQLite？](#21-什么是-sqlite)
2. [2.2 环境搭建](#22-环境搭建)
3. [2.3 第一个 SQLite 程序](#23-第一个-sqlite-程序)
4. [2.4 使用 sqlite3 命令行工具](#24-使用-sqlite3-命令行工具)
5. [2.5 本章小结](#25-本章小结)

### 本章会学到什么

学完这一章后，你应该能建立下面这些基础能力：

1. 说清楚 SQLite 和 MySQL、PostgreSQL 这类服务端数据库在架构上的差别。
2. 在本机安装并验证 SQLite 开发环境。
3. 编译并运行第一个 SQLite C/C++ 程序。
4. 看懂最基本的 `open -> use -> close` API 使用模式。
5. 用 `sqlite3` 命令行工具直接检查数据库文件内容。

## 2.1 什么是 SQLite？

SQLite 是一个**嵌入式关系型数据库引擎**。与 MySQL、PostgreSQL 等"客户端-服务器"架构的数据库不同，SQLite 具有以下显著特点：

### 核心特性

| 特性 | 说明 | 对嵌入式工程师的意义 |
|------|------|---------------------|
| **零配置** | 无需安装、无需启动服务 | 不占用额外系统资源 |
| **单文件存储** | 整个数据库就是一个 `.db` 文件 | 可以直接拷贝/备份 |
| **进程内运行** | 以库的形式链接到你的程序中 | 无 IPC 开销，调用就像调用函数 |
| **跨平台** | 支持 Linux/Windows/macOS/嵌入式 | 一份代码到处运行 |
| **轻量** | 编译后约 300-700KB | 适合资源受限系统 |
| **公有领域** | 无版权、无 License 限制 | 可自由用于商业产品 |

### 架构对比

```
传统数据库（如 MySQL）：            SQLite：

┌──────────┐                    ┌──────────────────┐
│ 你的应用程序 │                    │   你的应用程序      │
└─────┬────┘                    │  ┌────────────┐  │
      │ 网络/Socket              │  │ SQLite 库   │  │
┌─────┴────┐                    │  └──────┬─────┘  │
│ 数据库服务器 │                    └────────┬─────────┘
│  (独立进程) │                             │ 直接文件 I/O
└─────┬────┘                    ┌──────────┴──────┐
      │                        │  database.db 文件 │
┌─────┴────┐                    └─────────────────┘
│ 数据库文件  │
└──────────┘
```

### SQLite 的应用场景

SQLite 是世界上部署最广泛的数据库，你可能每天都在使用它：

- **Android/iOS**：每个 App 的本地数据存储
- **浏览器**：Chrome/Firefox 的书签、历史记录
- **嵌入式设备**：IoT 网关的数据记录、车载系统的配置管理
- **桌面软件**：邮件客户端、音乐播放器的元数据管理

很多初学者第一次接触 SQLite 时，会误以为它只是“轻量版 MySQL”。这个理解不准确。SQLite 的关键不只是体积小，而是它的部署模型完全不同：它没有单独的数据库进程，没有端口监听，没有用户权限系统这类服务端运维成本。它更像是一个带事务和查询能力的本地数据引擎。

这正是它特别适合嵌入式系统的原因：

1. 部署简单，只要把库链接进程序即可。
2. 调用路径短，程序直接通过函数访问数据库。
3. 故障面更小，不需要再维护一个独立数据库服务是否启动成功。
4. 数据文件天然便于打包、备份和迁移。

## 2.2 环境搭建

### 安装 SQLite 开发库

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install libsqlite3-dev sqlite3

# 验证安装
sqlite3 --version
pkg-config --libs sqlite3
```

如果 `sqlite3 --version` 能输出版本号，说明命令行工具已经安装成功；如果 `pkg-config --libs sqlite3` 能输出类似 `-lsqlite3` 的结果，说明编译阶段也能找到链接信息。这两个检查分别覆盖了“运行工具”和“编译程序”两个不同层面。

### 项目结构

本教程使用 CMake 构建，项目根目录的 `CMakeLists.txt` 已配置好所有示例的编译规则。

你可以把这个项目理解成“文档 + 示例代码 + 可执行程序”的对应关系：

- `docs/` 负责讲解概念和 API 背景。
- `examples/` 放每一章对应的 C++ 示例。
- `build/` 中生成每章可直接运行的可执行文件。

这样学习时就能形成闭环：先读文档建立概念，再看代码理解调用方式，最后运行程序观察实际输出。

### 构建与运行

```bash
cd sqlite_demo
mkdir build && cd build
cmake ..
make -j$(nproc)

# 运行本章示例
./ch02_hello_sqlite
```

如果构建失败，优先检查三件事：

1. 系统是否安装了 `libsqlite3-dev`。
2. CMake 是否成功找到 `sqlite3` 头文件和库。
3. 你当前运行的是否是 `build/` 目录下生成的目标程序。

## 2.3 第一个 SQLite 程序

让我们写一个最简单的程序：打开（创建）一个数据库文件，然后关闭它。

### 示例代码

文件：`examples/ch02_hello_sqlite.cpp`

```cpp
#include <sqlite3.h>
#include <cstdio>

int main() {
    sqlite3 *db = nullptr;

    // 打开（或创建）数据库文件
    // 如果 test.db 不存在，SQLite 会自动创建它
    int rc = sqlite3_open("test.db", &db);

    if (rc != SQLITE_OK) {
        fprintf(stderr, "无法打开数据库: %s\n", sqlite3_errmsg(db));
        sqlite3_close(db);
        return 1;
    }

    printf("成功打开数据库！\n");
    printf("SQLite 版本: %s\n", sqlite3_libversion());

    // 使用 sqlite3_exec 执行一条简单的 SQL 查看版本
    sqlite3_stmt *stmt = nullptr;
    rc = sqlite3_prepare_v2(db, "SELECT sqlite_version();", -1, &stmt, nullptr);
    if (rc == SQLITE_OK && sqlite3_step(stmt) == SQLITE_ROW) {
        printf("SQL 查询返回的版本: %s\n", sqlite3_column_text(stmt, 0));
    }
    sqlite3_finalize(stmt);

    // 关闭数据库
    sqlite3_close(db);
    printf("数据库已关闭。\n");

    // 清理测试文件
    remove("test.db");

    return 0;
}
```

### 代码解析

这个简短的程序包含了 SQLite C API 的基本使用模式：

#### 1. 包含头文件

```cpp
#include <sqlite3.h>
```

这是 SQLite 的唯一头文件，包含了所有需要的函数声明和常量定义。

这也体现了 SQLite C API 的一个特点：入口集中、接口统一。你不需要像某些大型库那样包含很多模块化头文件，绝大多数日常开发都从这个头文件开始。

#### 2. 打开数据库

```cpp
sqlite3 *db = nullptr;
int rc = sqlite3_open("test.db", &db);
```

- `sqlite3` 是数据库连接的不透明指针类型（类似嵌入式中的外设句柄）
- `sqlite3_open()` 打开或创建一个数据库文件
- 返回 `SQLITE_OK`（值为 0）表示成功，其他值表示错误

这里最重要的心智模型是：`db` 不是数据库内容本身，而是“这次连接”的句柄。后续所有操作，例如执行 SQL、准备语句、读取错误信息，都是围绕这个句柄展开的。

#### 3. 错误处理

```cpp
if (rc != SQLITE_OK) {
    fprintf(stderr, "无法打开数据库: %s\n", sqlite3_errmsg(db));
}
```

- `sqlite3_errmsg()` 返回最近一次错误的描述字符串
- 即使打开失败，也需要调用 `sqlite3_close()` 来释放资源

初学时建议养成一个习惯：**每次调用 SQLite API 后都先看返回值，再决定下一步**。SQLite 的 API 风格很统一，大多数函数都会通过返回码告诉你这一步是否成功，所以错误处理不应该放到最后“统一再看”，而应该紧跟在调用之后。

#### 4. 关闭数据库

```cpp
sqlite3_close(db);
```

- **必须在程序退出前关闭数据库**，否则可能导致数据丢失
- 类似于嵌入式中关闭文件描述符或释放外设资源

虽然操作系统在进程退出时也会回收很多资源，但工程上不应该依赖这种“顺带清理”。显式关闭数据库不仅更安全，也能让你的程序结构更清晰，尤其是在后面引入事务、预编译语句和多模块封装之后。

### API 使用模式总结

```
sqlite3_open()    →  获得数据库句柄
    │
    ├── sqlite3_prepare_v2()  →  编译 SQL 语句
    │       │
    │       ├── sqlite3_step()  →  执行/获取下一行
    │       │
    │       └── sqlite3_finalize()  →  释放编译好的语句
    │
    └── sqlite3_close()  →  关闭数据库
```

这个模式和嵌入式开发中的资源管理非常类似：
- `open` → `use` → `close`
- 就像 UART：`uart_init()` → `uart_read/write()` → `uart_deinit()`

后面章节会把这个模型进一步展开成更细的资源生命周期：

1. 打开数据库连接。
2. 准备 SQL 语句。
3. 绑定参数。
4. 执行并读取结果。
5. 释放语句对象。
6. 关闭数据库连接。

如果你现在先把这条主线记住，后续学习就会顺很多。

## 2.4 使用 sqlite3 命令行工具

SQLite 附带一个命令行工具 `sqlite3`，可以交互式地操作数据库，非常适合学习和调试：

```bash
# 创建/打开一个数据库
sqlite3 mytest.db

# 在 sqlite3 shell 中执行：
sqlite> .help                    -- 查看帮助
sqlite> .tables                  -- 列出所有表
sqlite> SELECT sqlite_version(); -- 查看版本
sqlite> .quit                    -- 退出
```

这个工具在调试时很有用——你可以直接打开程序生成的 `.db` 文件来检查数据是否正确。

建议把 `sqlite3` shell 当成数据库世界里的“示波器”或“串口调试助手”。当你怀疑程序没有正确建表、插入失败、查询条件写错时，最直接的办法往往不是先读一大堆代码，而是先打开数据库文件亲眼看一眼：

- 表到底有没有创建出来。
- 某条记录到底有没有插进去。
- 某个字段的实际值是不是你以为的那样。

这种“先看数据库当前状态，再反推程序行为”的调试方式，在后续章节里会非常常用。

## 2.5 本章小结

- SQLite 是一个零配置、单文件、进程内的嵌入式数据库
- 使用 `sqlite3_open()` / `sqlite3_close()` 管理数据库连接
- 所有 SQLite API 返回整型错误码，`SQLITE_OK` 表示成功
- 资源管理模式与嵌入式外设操作类似

### 把本章浓缩成一套心智模型

如果要把第二章压缩成最核心的几句话，可以记成：

1. SQLite 不是独立运行的数据库服务，而是直接链接进程序里的数据库库。
2. 你的程序通过函数调用而不是网络协议来使用它。
3. 第一个 SQLite 程序的基本节奏就是：打开数据库、执行操作、检查返回值、关闭数据库。
4. `sqlite3` 命令行工具是学习和调试 SQLite 的重要辅助工具。

### 从本章进入下一章前，你应该已经掌握什么

如果这一章已经掌握，你应该能够：

1. 区分 SQLite 和客户端-服务器数据库的基本架构差异。
2. 独立搭建并验证 SQLite 开发环境。
3. 编译运行一个最小的 SQLite 示例程序。
4. 读懂最基本的数据库连接生命周期和错误检查写法。

---

[⬅ 第一章：数据库基础概念](ch01_database_basics.md) | [目录](../README.md) | [第三章：SQL 基本操作 ➡](ch03_basic_sql.md)
